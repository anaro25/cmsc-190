
def run_astar(composite_grid):
	
	# write a function that accepts the initial state and does A* algo.
	# The grid's versions for each timestep are each recorded in a list of composite_grid called astar_frames
	
	return astar_frames